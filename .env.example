# No environment variables are required to run this app locally.
# Firebase client configuration lives in firebase-applet-config.json
# (client-side Firebase keys are public by design; access is controlled
# by firestore.rules).
